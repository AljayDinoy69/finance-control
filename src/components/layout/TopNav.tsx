import { Bell, Menu, Moon, Search, Sun } from 'lucide-react'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { useLocation } from 'react-router-dom'
import { useTheme } from '../../context/ThemeContext'
const labels: Record<string, string> = { '/dashboard': 'Overview', '/transactions': 'Transactions', '/budgets': 'Budgets', '/reports': 'Reports', '/settings': 'Settings' }
export default function TopNav({ onMenu }: { onMenu: () => void }) { const { theme, setTheme, toggleTheme } = useTheme(); const { pathname } = useLocation(); return <header className="flex min-h-20 items-center justify-between gap-3 border-b border-border bg-background/80 px-4 py-3 backdrop-blur sm:px-5 md:px-8"><div className="flex items-center gap-4"><button className="grid size-10 shrink-0 place-items-center rounded-lg hover:bg-accent md:hidden" onClick={onMenu} aria-label="Open menu"><Menu className="size-5" /></button><div><p className="text-xs text-muted-foreground">Workspace /</p><h1 className="text-lg font-semibold tracking-tight">{labels[pathname] || 'Overview'}</h1></div></div><div className="flex items-center gap-2"><button className="hidden rounded-lg p-2 text-muted-foreground hover:bg-accent hover:text-foreground sm:block" aria-label="Search"><Search className="size-4" /></button><Select value={theme} onValueChange={(value) => value && setTheme(value as 'light' | 'dark' | 'system')}><SelectTrigger className="hidden h-9 w-28 sm:flex"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="light"><Sun data-icon="inline-start" /> Light</SelectItem><SelectItem value="dark"><Moon data-icon="inline-start" /> Dark</SelectItem><SelectItem value="system">System</SelectItem></SelectContent></Select><button className="rounded-lg p-2 text-muted-foreground hover:bg-accent hover:text-foreground" onClick={toggleTheme} aria-label="Cycle theme"><Sun className="size-4" /></button><button className="rounded-lg p-2 text-muted-foreground hover:bg-accent hover:text-foreground" aria-label="Notifications"><Bell className="size-4" /></button><div className="ml-2 grid size-9 place-items-center rounded-full bg-primary text-xs font-bold text-primary-foreground">AM</div></div></header> }
